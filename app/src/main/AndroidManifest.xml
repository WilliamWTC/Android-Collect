<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.collect">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_collect_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Collect">
        <activity android:name=".NotesProgress"></activity>
        <activity
            android:name=".AddItem"
            android:label="@string/title_activity_add_item"
            android:theme="@style/Theme.Collect.NoActionBar" />
        <activity
            android:name=".ObjectDetails"
            android:label="@string/title_activity_object_details"
            android:theme="@style/Theme.Collect.NoActionBar" />
        <activity android:name=".Category" />
        <activity android:name=".HomePage" />
        <activity android:name=".RegisterUser" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>